import React, { Component } from "react"

export default class PaymentModal extends Component {
  constructor(props) {
    super(props)
    this.state = {}
  }

  render() {
    return <div className="paymentModal">Payment Modal</div>
  }
}
